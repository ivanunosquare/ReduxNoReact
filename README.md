#Redux without React"

* Instructions
   
   * node js installed
   * over the root folder npm install
   * over the client folder npm install
   * over the root run nodemon server
   * open the browser and type http://localhost:3000/
   * please install in google chrome redux extension for see the states changes
   
   
   